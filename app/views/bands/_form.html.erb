<%= form_with(model: band) do |form| %>
  <% if band.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
        <% band.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :band_id, { value:params[:id] }%>

  <div>
    <%= form.label :band_name, style: "display: block" %>
    <%= form.text_area :band_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
